1. Add your Supabase URL and key in index.js and order.js.
2. Import schema.sql and functions.sql into Supabase.
3. Run `npm install`.
4. Run `node backend/server.js`.
5. Open frontend/index.html in browser.